# dotfiles setup

# Create symlinks in the home dir
./setup.sh

# Required to use all functionalities in dotfiles

# install homebrew
# Search the command in brew.sh

# Fuzzy search
brew install fd ripgrep fzf
brew install neovim
brew install nvm
nvm install node

# nvim -> :Mason
# Install
# - lua-language-server
# - pyright-langserver
# - typescript-language-server
